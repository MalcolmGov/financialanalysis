# ── Shared ────────────────────────────────────────────────────────────────────
# Neon Postgres (EU: aws-eu-central-1 recommended; region fixed at creation)
DATABASE_URL=postgres://user:pass@host/db?sslmode=require

# Vercel Blob — PRIVATE store only (EU region; fixed at creation)
BLOB_READ_WRITE_TOKEN=

# ── apps/portal ───────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=
AUTH_SECRET=                       # openssl rand -base64 32
AUTH_RESEND_KEY=                   # Resend API key for magic links
AUTH_EMAIL_FROM=studio@example.com # decide sending domain (open decision #6)
OPERATOR_EMAILS=you@example.com    # comma-separated allowlist
WORKER_BASE_URL=http://localhost:8000
WORKER_SERVICE_TOKEN=              # shared secret, portal -> worker
WEBHOOK_HMAC_KEY=                  # worker -> portal webhook signing
APP_BASE_URL=http://localhost:3000

# ── services/worker ───────────────────────────────────────────────────────────
# Also needs DATABASE_URL, BLOB_READ_WRITE_TOKEN, WORKER_SERVICE_TOKEN, WEBHOOK_HMAC_KEY
